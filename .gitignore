# 凭据：绝不入 git
.env

# 运行期产物：本地留档，不入 git
data/
logs/
outbox/
inbox/          # 外部工具投递的原始报告（Reddit 爬取报告等），体积大且含第三方原文

# Python
.venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# Phase 3: skill 暂存目录（每次运行前由 skill_check.py --stage 重建，不入 git）
.claude/skills/
